name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: ELEON1116/eleon_automerged@master
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          number: 1
#
